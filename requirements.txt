# tts-skill 依赖（运行时）
# 注意：PyTorch 由 setup_env.py 根据平台和 GPU 自动选择安装（CUDA/MPS/XPU/CPU）
# 此文件仅用于开发环境基础依赖

omnivoice>=0.2.1
gradio>=4.0.0
soundfile
numpy

# 语音转文字（ASR）
faster-whisper>=1.0.0
imageio-ffmpeg>=0.4.9  # 提供跨平台 ffmpeg 二进制，用于视频提取音轨
